name: Keep Backend Awake
on:
  schedule:
    - cron: '*/5 * * * *' 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend warmup
        run: |
          echo "Pinging warmup... "
          curl -m 10 -sSf https://relife-y0xi.onrender.com/api/warmup || echo "ping-failed"